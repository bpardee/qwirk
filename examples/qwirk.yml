in_mem:
  :adapter: InMem
  :log_times: true

active_mq:
  :adapter: ActiveMQ
  :log_times: true
  :factory: org.apache.activemq.ActiveMQConnectionFactory
  :broker_url: tcp://localhost:61616
  #:username: myuser
  #:password: mypassword

#active_mq_invm:
#  :adapter: ActiveMQ
#  :log_times: true
#  :factory: org.apache.activemq.ActiveMQConnectionFactory
#  :broker_url: vm://claritybase?brokerConfig=xbean:< %= Rails.root.join('config', 'activemq.xml') % >
#  :object_message_serialization_defered: true
